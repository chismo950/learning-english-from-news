# Copy this file to .env.local and fill in with your own credentials.
# OpenAI API key used for generating language learning content.
OPENAI_API_KEY=your-openai-api-key

# Upstash KV credentials for storing and retrieving cached responses.
KV_REST_API_TOKEN=your-upstash-kv-rest-api-token
KV_REST_API_URL=https://your-upstash-instance.upstash.io

# JSON array of Google Generative AI API keys; keep at least one entry.
GOOGLE_GENERATIVE_AI_API_KEYS=[ "your-google-generative-ai-key" ]
